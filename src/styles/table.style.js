import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  inner: {},
  outer: {
    backgroundColor: '#fff',
    marginHorizontal: 20,
    marginTop: 20,
    paddingBottom: 20,
  },
});
